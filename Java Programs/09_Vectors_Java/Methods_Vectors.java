import java.util.*;

public class Methods_Vectors {
    public static void main(String[] args) {
        Vector<Integer> obj = new Vector<>();
        // obj is used to access methods of the Vector Class
        /*
        Some of the Useful Methods of Vectors are 
        1. v.size(); --> Tells the size of the Vector
        2. v.capacity(); --> Gives the Total capacity of the Vector v
        3. v.add(data_types); --> add elements to the end of the Vector v 
        4. v.add(index i , element) ;--> add elements at specified location and shits the existing  element to right
        5. v.clear(); --> clears all the elements of the vectors 
        6. v.get(index i) ; --> gives the elements at specified location also for arraylist
        7. v.elementAt(index i ); --> Does the same work as get but only for Vectors
         */
    }
}

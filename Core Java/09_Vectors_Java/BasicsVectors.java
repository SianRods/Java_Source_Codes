import java.util.Vector;
// Vectors are Resizable Arrays --> Dynamic Array
// They have a by default capacity of 10 and each time a element beyond this capacity is its size increases by 100 %. Vectors are referenced data types and have different methods to use them ;

/**
 * BasicsVectors --> Vectors was introduced in JDK 1.0 whereas List was introduced in JDK1.2
 */
public class BasicsVectors {
    public static void main(String[] args) {
        // Initialisation of Arrays 
        // Vector<ref_data_type> object_name = new Vector<Data_type_ref>();
        // The initialisation is same as an Array list 
    }
    
}